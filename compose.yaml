services:
  catalogdb:
    image: postgres

  catalogapi:
    image: catalogapi
    build:
      context: .
      dockerfile: CatalogApi/Dockerfile
    # args:
    #   - configuration=Debug

  basketdb:
    image: postgres

  basketapi:
    image: basketapi
    build:
      context: .
      dockerfile: BasketApi/Dockerfile

  discountgrpc:
    image: discountgrpc
    build:
      context: .
      dockerfile: DiscountGrpc/Dockerfile

  orderdb:
    image: mcr.microsoft.com/mssql/server

  orderapi:
    image: orderapi
    build:
      context: .
      dockerfile: Ordering/Infrastructure/Dockerfile

  distributedcache:
    image: redis

  messagebroker:
    image: rabbitmq:management

  yarpapigateway:
    image: yarpapigateway
    build:
      context: .
      dockerfile: YarpApiGateway/Dockerfile

# volumes:
#   postgres_catalog:
